<!DOCTYPE html>
<html>

<head>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <h1>this is <%= userType %>  page</h1>
  <% if (userType && userType == 'admin')  {%>
  <% for (let i = 0; i < datas.length; i++) {%>
  <p><%= datas[i].productName %> </p>
  <%}}%>
</body>

</html>
